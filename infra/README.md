# Infrastructure

Local infrastructure configuration for development and integration testing.

- `postgres/`: init SQL and local DB setup
- `minio/`: bucket bootstrap and storage notes
- `qdrant/`: local Qdrant config
- `nginx/`: reverse proxy config if needed later
- `docker/`: shared container scripts if needed later
